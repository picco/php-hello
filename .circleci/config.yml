version: 2.1
jobs:
  build:
    docker:
      - image: google/cloud-sdk
    steps:
      - add_ssh_keys: 
          fingerprints: 
            - "94:8e:35:11:3e:19:e4:29:39:f9:d8:d6:4b:53:50:06"    
      - run: |
          gcloud version
          echo $GCLOUD_SERVICE_KEY | gcloud auth activate-service-account --key-file=-
          gcloud --quiet config set project ${GOOGLE_PROJECT_ID}
          gcloud --quiet config set compute/zone ${GOOGLE_COMPUTE_ZONE}    
          gsutil
      - checkout
      - run: |
          echo "great success!"
      - run: |      
          scp -r ./* dev@console.fenomen.ee:/mnt/dev/www/deployments/php-hello
